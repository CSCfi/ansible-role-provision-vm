ram: 4096
network_address: "192.168.1.51"
networks:
  - "network --onboot=on --device=eth0 --bootproto=static --ip={{ network_address }} --netmask={{ network_mask }} --gateway={{ network_gateway4 }} --nameserver={{ dns1 }} --hostname={{ ansible_hostname }}"