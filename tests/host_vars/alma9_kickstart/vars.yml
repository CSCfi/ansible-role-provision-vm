vcpus: 2
ram: 2048
internal_interface: enp1s0
debug: true
network_address: "192.168.1.53"
networks:
  - "network --onboot=on --device={{ internal_interface }} --bootproto=static --ip={{ network_address }} --netmask={{ network_mask }} --gateway={{ network_gateway4 }} --nameserver={{ dns|join(',') }} --hostname={{ inventory_hostname }}"
