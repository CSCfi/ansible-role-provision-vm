guest_type: image
ram: 1536
internal_interface: eth0
network_address: "192.168.1.50"
ethernets:
-   name: "{{ internal_interface }}"
    address4: "{{ network_address }}/24"
    gateway4: "{{ network_gateway4 }}"
    nameservers:
    - "{{ dns1 }}"
